package com.pesu.hotelmanagement.controller;

/**
 * This REST controller has been superseded by AdminController (/admin/suites/*)
 * and is kept here only as a placeholder to avoid breaking any external API clients.
 *
 * All room management is now handled through the MVC admin panel with
 * proper session-based authentication and RoomStatus state machine support.
 */
public class RoomController {
    // Intentionally empty — see AdminController for room management endpoints.
}
